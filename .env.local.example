# Encryption key for credential storage (32+ characters)
# Generate with: openssl rand -hex 32
NEXT_PUBLIC_ENCRYPTION_SECRET=your-secret-key-here-replace-with-actual-key

# Convex
NEXT_PUBLIC_CONVEX_URL=your-convex-url
CONVEX_DEPLOYMENT=your-deployment
